export type Pagination = {
  page: number,
  count: number,
  totalCount: number,
  pageCount: number,
}

export type Timestamp = {
  day: number,
  month: number,
  year: number,
  hours: number,
  minutes: number,
  seconds: number,
}
